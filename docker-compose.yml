version: '3'
services:
  site:
    build: .
    command: sh -c "bundle exec jekyll serve --host 0.0.0.0 --livereload"
    volumes:
      - .:/srv/jekyll:cached
    ports:
      - "4000:4000"
    environment:
      - JEKYLL_ENV=development